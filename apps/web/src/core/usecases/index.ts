/**
 * Use-cases layer (master spec Section 5.1): optional thin orchestration over
 * ports. Use cases depend on `core/ports` and `core/domain` only, never on
 * infrastructure or framework code. Introduced per approved work unit.
 */
export {};
